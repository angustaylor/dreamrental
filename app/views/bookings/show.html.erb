<p id="notice"><%= notice %></p>

<div class="container">

  <div class ="row d-flex justify-content-between align-items-baseline">
    <h1>Booking Details</h1>
    <p>
      <% if @booking.user == current_user || current_user.present? && current_user.admin? %>
        <%= link_to 'Edit', edit_booking_path() %>
      <% end %>
      <% if @booking.user == current_user || current_user.present? && current_user.admin? %>
        <%= link_to 'Destroy', booking_path, method: :delete, data: { confirm: 'Are you sure?' } %>
      <% end %>
      <%= link_to 'Back', bookings_path %>
    </p>
  </div>
  <div>
    <p>Start Date: <%= @booking.start_date %></p>
    <p>End Date: <%= @booking.end_date %></p>
    <p>Total Cost: <%= @booking.amount %></p>
  </div>

  <div>
    <%= link_to 'Leave a review', new_booking_review_path(@booking) %>
  </div>


</div>
